// ~~ Generated by projen. To modify, edit .projenrc.ts and run "npx projen".
import * as path from 'path';
import * as lambda from 'aws-cdk-lib/aws-lambda';
import { Construct } from 'constructs';

/**
 * Props for SportFunction
 */
export interface SportFunctionProps extends lambda.FunctionOptions {
}

/**
 * An AWS Lambda function which executes src/app/sport/sport.
 */
export class SportFunction extends lambda.Function {
  constructor(scope: Construct, id: string, props?: SportFunctionProps) {
    super(scope, id, {
      description: 'src/app/sport/sport.lambda.ts',
      ...props,
      runtime: new lambda.Runtime('nodejs24.x', lambda.RuntimeFamily.NODEJS),
      handler: 'index.handler',
      code: lambda.Code.fromAsset(path.join(__dirname, '../../../assets/app/sport/sport.lambda')),
    });
    this.addEnvironment('AWS_NODEJS_CONNECTION_REUSE_ENABLED', '1', { removeInEdge: true });
  }
}