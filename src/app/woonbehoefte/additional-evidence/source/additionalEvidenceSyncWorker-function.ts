// ~~ Generated by projen. To modify, edit .projenrc.ts and run "npx projen".
import * as path from 'path';
import * as lambda from 'aws-cdk-lib/aws-lambda';
import { Construct } from 'constructs';

/**
 * Props for AdditionalEvidenceSyncWorkerFunction
 */
export interface AdditionalEvidenceSyncWorkerFunctionProps extends lambda.FunctionOptions {
}

/**
 * An AWS Lambda function which executes src/app/woonbehoefte/additional-evidence/source/additionalEvidenceSyncWorker.
 */
export class AdditionalEvidenceSyncWorkerFunction extends lambda.Function {
  constructor(scope: Construct, id: string, props?: AdditionalEvidenceSyncWorkerFunctionProps) {
    super(scope, id, {
      description: 'src/app/woonbehoefte/additional-evidence/source/additionalEvidenceSyncWorker.lambda.ts',
      ...props,
      runtime: new lambda.Runtime('nodejs24.x', lambda.RuntimeFamily.NODEJS),
      handler: 'index.handler',
      code: lambda.Code.fromAsset(path.join(__dirname, '../../../../../assets/app/woonbehoefte/additional-evidence/source/additionalEvidenceSyncWorker.lambda')),
    });
    this.addEnvironment('AWS_NODEJS_CONNECTION_REUSE_ENABLED', '1', { removeInEdge: true });
  }
}