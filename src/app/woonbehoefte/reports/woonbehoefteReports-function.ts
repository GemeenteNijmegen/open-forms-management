// ~~ Generated by projen. To modify, edit .projenrc.ts and run "npx projen".
import * as path from 'path';
import * as lambda from 'aws-cdk-lib/aws-lambda';
import { Construct } from 'constructs';

/**
 * Props for WoonbehoefteReportsFunction
 */
export interface WoonbehoefteReportsFunctionProps extends lambda.FunctionOptions {
}

/**
 * An AWS Lambda function which executes src/app/woonbehoefte/reports/woonbehoefteReports.
 */
export class WoonbehoefteReportsFunction extends lambda.Function {
  constructor(scope: Construct, id: string, props?: WoonbehoefteReportsFunctionProps) {
    super(scope, id, {
      description: 'src/app/woonbehoefte/reports/woonbehoefteReports.lambda.ts',
      ...props,
      runtime: new lambda.Runtime('nodejs24.x', lambda.RuntimeFamily.NODEJS),
      handler: 'index.handler',
      code: lambda.Code.fromAsset(path.join(__dirname, '../../../../assets/app/woonbehoefte/reports/woonbehoefteReports.lambda')),
    });
    this.addEnvironment('AWS_NODEJS_CONNECTION_REUSE_ENABLED', '1', { removeInEdge: true });
  }
}