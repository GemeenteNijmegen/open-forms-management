// ~~ Generated by projen. To modify, edit .projenrc.ts and run "npx projen".
import * as path from 'path';
import * as lambda from 'aws-cdk-lib/aws-lambda';
import { Construct } from 'constructs';

/**
 * Props for PermissionsFunction
 */
export interface PermissionsFunctionProps extends lambda.FunctionOptions {
}

/**
 * An AWS Lambda function which executes src/app/permissions/permissions.
 */
export class PermissionsFunction extends lambda.Function {
  constructor(scope: Construct, id: string, props?: PermissionsFunctionProps) {
    super(scope, id, {
      description: 'src/app/permissions/permissions.lambda.ts',
      ...props,
      runtime: new lambda.Runtime('nodejs24.x', lambda.RuntimeFamily.NODEJS),
      handler: 'index.handler',
      code: lambda.Code.fromAsset(path.join(__dirname, '../../../assets/app/permissions/permissions.lambda')),
    });
    this.addEnvironment('AWS_NODEJS_CONNECTION_REUSE_ENABLED', '1', { removeInEdge: true });
  }
}